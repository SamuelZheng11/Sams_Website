export interface IEducation {
    startDate: Date,
    endDate: Date,
    institutionName: string,
    institutionShortHand: string,
    institutionWebsite: string,
    summaries: string[],
    achievements: string[]
}